(()=>{let lastKey='';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const num=n=>Number(n?.value??n??0).toLocaleString();async function load(){const view=document.querySelector('#view-analytics');if(!view||view.classList.contains('hidden'))return;const wid=window.state?.workspace?.id||'';const key=wid+':'+Math.floor(Date.now()/30000);if(key===lastKey)return;lastKey=key;const panel=view.querySelector('.traffic-panel');if(!panel)return;try{const r=await fetch('/api/app/umami/analytics?days=30',{credentials:'same-origin',headers:wid?{'x-workspace-id':wid}:{}}),j=await r.json();if(!r.ok)throw Error(j.message||'Analytics unavailable');if(!j.connected){panel.innerHTML='<div class="panel-head"><div><p class="eyebrow">WEBSITE TRAFFIC</p><h2>Connect this client website</h2><p>Enter the client website domain above. SiteRemade will create and link its private analytics property automatically.</p></div><span class="status-pill">SETUP</span></div>';return;}const s=j.stats||{},active=j.active?.visitors??j.active?.value??j.active??0;const rows=(a,label)=>`<div class="v24-list"><strong>${label}</strong>${(Array.isArray(a)?a:[]).slice(0,6).map(x=>`<div><span>${esc(x.x||x.name||x.value||'Direct')}</span><b>${num(x.y??x.count??0)}</b></div>`).join('')||'<small>No data yet</small>'}</div>`;panel.innerHTML=`<div class="panel-head"><div><p class="eyebrow">WEBSITE TRAFFIC · LAST 30 DAYS</p><h2>${esc(j.domain)}</h2><p>Measured from this client’s own website through SiteRemade analytics.</p></div><span class="status-pill">LIVE · ${num(active)} NOW</span></div><div class="v23-results-grid"><article><span>VISITORS</span><strong>${num(s.visitors)}</strong><small>Unique measured visitors</small></article><article><span>VISITS</span><strong>${num(s.visits)}</strong><small>Website sessions</small></article><article><span>PAGEVIEWS</span><strong>${num(s.pageviews)}</strong><small>Pages viewed</small></article><article><span>BOUNCE RATE</span><strong>${s.bounces&&s.visits?Math.round((Number(s.bounces.value??s.bounces)/Number(s.visits.value??s.visits))*100)+'%':'0%'}</strong><small>Single-page visits</small></article></div><div class="v24-breakdown">${rows(j.pages,'Top pages')}${rows(j.referrers,'Referrers')}${rows(j.devices,'Devices')}${rows(j.countries,'Countries')}</div>`;}catch(e){panel.innerHTML='<div class="panel-head"><div><p class="eyebrow">WEBSITE TRAFFIC</p><h2>Analytics temporarily unavailable</h2><p>'+esc(e.message)+'</p></div></div>';}}
function install(){if(typeof window.renderAll!=='function'||typeof window.state==='undefined')return setTimeout(install,100);const old=window.renderAll;window.renderAll=function(){const r=old.apply(this,arguments);setTimeout(load,20);return r;};document.addEventListener('click',e=>{if(e.target.closest('[data-view="analytics"]'))setTimeout(()=>{lastKey='';load()},80)});setTimeout(load,250);}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();})();
